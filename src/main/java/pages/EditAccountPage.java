package pages;

import org.openqa.selenium.WebDriver;

public class EditAccountPage extends BasePage {
    // Locators
    // Weberror 500

    // Constructor
    public EditAccountPage(WebDriver driver) {
        super(driver);
    }

    // Actions
    // Write actions after getting locators
}
